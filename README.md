# Chip8 Interpreter

A simple Chip8 interpreter written in C++.

### Compiling the interpreter

On the source of the project, run:

`$ make`

SLD2 must be installed in order for the compilation to succeed.

### Running the interpreter

`$ ./chip8 <Scale> <Delay> <ROM_Path>`

Some example ROMs are given in the `/roms` directory.